# 🦝 小浣熊知识库社区平台 - 快速开始指南

## 已完成的工作

✅ **项目基础架构**
- Turborepo Monorepo 配置
- pnpm workspace 设置
- TypeScript 配置

✅ **前端应用 (Next.js 14)**
- App Router 结构
- Tailwind CSS 配置
- 基础页面（首页、登录、注册、Dashboard）
- 路由组织（auth、main）

✅ **后端服务 (Fastify)**
- API 服务器基础架构
- 中间件（认证、错误处理）
- 插件集成（CORS、JWT、限流）

✅ **数据库层 (Drizzle ORM)**
- 完整的 Schema 定义（用户、知识库、文档、权限、评论、互动）
- 数据库关系配置
- 迁移工具配置

✅ **共享包**
- 类型定义
- 常量定义
- Zod 验证 Schema
- 工具函数

✅ **开发环境**
- Docker Compose（PostgreSQL + Redis）
- 环境变量模板

## 下一步：安装依赖和启动项目

### 1. 安装 pnpm（如果还没安装）

```bash
npm install -g pnpm@9.15.4
```

### 2. 安装项目依赖

进入项目目录：
```bash
cd /Users/asui/raccoon-kb-mvp
```

安装所有依赖：
```bash
pnpm install
```

### 3. 启动数据库

```bash
docker-compose up -d
```

### 4. 配置环境变量

复制环境变量模板：
```bash
cp .env.example .env
```

如果需要，编辑 `.env` 文件修改数据库连接等配置。

### 5. 推送数据库 Schema

```bash
pnpm db:push
```

### 6. 启动开发服务器

```bash
pnpm dev
```

这将同时启动：
- 前端：http://localhost:3000
- API：http://localhost:3001

## 验证安装

1. **访问前端**：打开浏览器访问 http://localhost:3000
   - 应该看到小浣熊知识库首页
   - 可以导航到登录/注册页面

2. **测试 API**：
   ```bash
   curl http://localhost:3001/health
   ```
   应该返回：`{"status":"ok","timestamp":"..."}`

## 项目结构

```
raccoon-kb-mvp/
├── apps/
│   ├── web/          # Next.js 前端
│   │   ├── app/      # App Router
│   │   ├── components/
│   │   ├── lib/
│   │   └── hooks/
│   └── api/          # Fastify API
│       └── src/
│           ├── modules/
│           ├── plugins/
│           └── middleware/
├── packages/
│   ├── db/           # Drizzle ORM
│   └── shared/       # 共享代码
└── docker-compose.yml
```

## 可用命令

```bash
# 开发
pnpm dev              # 启动所有服务
pnpm build            # 构建所有包
pnpm lint             # 代码检查
pnpm type-check       # 类型检查

# 数据库
pnpm db:push          # 推送 schema 到数据库
pnpm db:studio        # 打开 Drizzle Studio
pnpm db:migrate       # 运行迁移
```

## 下一步开发任务

根据 MVP 计划，接下来需要实现：

1. **用户认证系统**（Week 1 剩余任务）
   - NextAuth.js 配置
   - JWT 认证实现
   - 注册/登录 API

2. **知识库管理**（Week 2）
   - 知识库 CRUD API
   - 权限控制
   - 前端知识库管理界面

3. **文档管理**（Week 3）
   - 文档 CRUD API
   - 树形结构展示

更多详细信息请查看 [计划文档](/Users/asui/.claude/plans/radiant-petting-giraffe.md)。

## 常见问题

### 端口冲突
如果端口 3000 或 3001 已被占用，可以修改：
- 前端：修改 `apps/web/package.json` 的 dev 脚本
- API：修改 `.env` 文件的 `PORT` 变量

### Docker 启动失败
确保 Docker 已安装并正在运行：
```bash
docker --version
docker-compose --version
```

### 数据库连接失败
检查 Docker 容器状态：
```bash
docker-compose ps
```

查看日志：
```bash
docker-compose logs postgres
```

## 技术支持

如有问题，请查看：
- 项目 README: [README.md](README.md)
- 完整计划: [/Users/asui/.claude/plans/radiant-petting-giraffe.md]
